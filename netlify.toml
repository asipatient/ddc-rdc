# Configuration Netlify - site DDC RDC (Next.js 16, adaptateur OpenNext auto)
[build]
  command = "node scripts/netlify-migrate.mjs && next build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "22"
